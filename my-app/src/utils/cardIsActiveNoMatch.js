import Game from "../components/Game";

function cardIsActiveNoMatch() {
    console.log(Game.deactivatedCardCount.current);

/* 
    const MyContext = React.createContext(defaultValue);
    Game.deactivatedCardCount.current = createContext(+1);
    Game.score.current -= 1;
    Game.time.current -= 3; */

  return ;

}

export default cardIsActiveNoMatch;